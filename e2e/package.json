{
  "name": "e2e",
  "version": "1.0.0",
  "description": "End-to-end integration tests - live API calls",
  "private": true,
  "scripts": {
    "test": "jest --verbose",
    "test:ci": "jest --verbose --ci"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testTimeout": 180000,
    "testMatch": ["**/tests/**/*.test.js"]
  }
}
